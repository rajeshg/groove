# Groove Environment Variables

# App
APP_URL=http://localhost:5173
NODE_ENV=production

# Database (SQLite)
# Development: file:./prisma/dev.db
# Production (Docker): file:/data/db/prod.db
DATABASE_URL="file:/data/db/prod.db"

# Email Configuration (Gmail SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=example@example.com
SMTP_PASS="dummy password"
EMAIL_FROM="Groove <example@example.com>"
